@model List<Assign5.Models.Departments>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().dno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().dname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().budget)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.budget)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.dno }) |
                @Html.ActionLink("Details", "Details", new { id = item.dno }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.dno })
            </td>*@
        </tr>
    }

</table>
